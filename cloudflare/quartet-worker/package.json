{
  "name": "alias-spy-games-quartet-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/index-chat.js && node --check src/index-lobby-resume.js && node --check src/lobby-resume.js && node --check src/engine.js && node --check src/catalog.js && npm test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
