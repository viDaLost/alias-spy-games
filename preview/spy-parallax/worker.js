const VERSION='spy-parallax-review-v1';
export default{async fetch(request,env){const u=new URL(request.url);if(u.pathname==='/__preview_version')return Response.json({version:VERSION,game:'spy',review:true,productionMainTouched:false,layers:8,transparentRasterLayers:true,continuousLoops:0,parallax:'event-driven-smoothed-raf',features:['scroll-depth','horizontal-peek','role-state','event-patrol']},{headers:{'cache-control':'no-store'}});const r=await env.ASSETS.fetch(request);if(r.status===404&&request.method==='GET'){const f=await env.ASSETS.fetch(new Request(new URL('/index.html',request.url)));if(f.ok)return new Response(f.body,{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store','x-spy-review':VERSION}})}const h=new Headers(r.headers);if((h.get('content-type')||'').includes('text/html')){h.set('cache-control','no-store');h.set('x-spy-review',VERSION)}else h.set('cache-control','public, max-age=31536000, immutable');return new Response(r.body,{status:r.status,headers:h})}}
