name: Obsolete Moses Nile V7.3.3 Preview

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  obsolete:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'This legacy V7.3.3 deploy is disabled.'
          echo 'Use deploy-moses-nile-v740-preview.yml for the active Cloudflare preview.'
