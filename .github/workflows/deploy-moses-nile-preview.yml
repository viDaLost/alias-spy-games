name: Obsolete Moses Nile Legacy Preview

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  obsolete:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'This legacy Moses Nile deploy is disabled.'
          echo 'Use deploy-moses-nile-v740-preview.yml for the active Cloudflare preview.'
