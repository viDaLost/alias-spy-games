name: Apply support center integration

on:
  push:
    branches: [agent/support-center-v1]
    paths:
      - '.github/workflows/apply-support-center.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply support center patch
        run: python3 scripts/apply-support-center.py
      - name: Validate patch markers
        run: node scripts/check-support-center.mjs
      - name: Commit integration
        shell: bash
        run: |
          set -euo pipefail
          # The patch script also updates CI labels/check hooks locally, but the
          # workflow token intentionally has no workflows permission. Keep the
          # existing workflow files untouched in this one-shot helper commit.
          git checkout -- .github/workflows/build-android-apk.yml .github/workflows/quality.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add cloudflare/app-core-worker/src/index-v4.js android-app/app/src/main/java/com/vidalost/biblegames/data/CloudRepository.kt android-app/app/src/main/java/com/vidalost/biblegames/App.kt android-app/app/build.gradle index.html
          git commit -m 'Integrate support center across web and Android'
          git push origin HEAD:agent/support-center-v1
