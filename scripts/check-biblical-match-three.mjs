import fs from 'node:fs';import path from 'node:path';import {createRequire} from 'node:module';
const root=process.cwd(),require=createRequire(import.meta.url),Core=require(path.join(root,'web/games/biblical-match-three-core.js'));const read=p=>fs.readFileSync(path.join(root,p),'utf8'),ok=(v,m)=>{if(!v)throw new Error(m)};
const data=JSON.parse(read('web/data/biblical_match_three_levels.json'));ok(data.rows===8&&data.cols===8,'8-column default required');ok(data.levels?.length===30,'30 levels required');const rowSet=new Set(data.levels.map(level=>Number(level.rows||data.rows)));ok([5,6,7,8].every(rows=>rowSet.has(rows)),'varied 5/6/7/8-row levels required');for(const level of data.levels){const rows=Number(level.rows||data.rows);ok(rows>=5&&rows<=8,`level ${level.id} rows invalid`);for(const group of level.blockers||[])for(const cell of group.cells||[])ok(Number(cell)>=0&&Number(cell)<rows*data.cols,`level ${level.id} blocker ${cell} outside ${rows}x${data.cols}`)}
const runtime=read('web/games/biblical-match-three-v10-runtime.js'),ux=read('web/games/biblical-match-three-v10-ux.js'),ui11=read('web/games/biblical-match-three-v11-ui.js'),loader=read('web/games/biblical-match-three-v5-loader.js'),effects=read('web/games/biblical-match-three-effects.js'),game=read('web/games/biblical-match-three.js'),css10=read('web/styles/biblical-match-three-v10.css'),css11=read('web/styles/biblical-match-three-v11.css'),launcher=read('web/js/biblical-match-three-launcher.js');
ok(runtime.includes('TIMED_SECONDS = 90')&&runtime.includes('data-v10-timed')&&runtime.includes('setInterval(updateTimedHud, 500)'),'timed mode / low-frequency timer missing');ok(runtime.includes('progress.unlocked')&&runtime.includes('workers\\.dev'),'preview unlock missing');ok(runtime.includes('nodeImportant')&&runtime.includes('addedNodes')&&!runtime.includes('attributeFilter: ["class"'),'runtime observer must ignore animation attribute churn');
ok(loader.includes('art.symbols.ark=direct("ark")')&&loader.includes('art.boosters.ark=direct("ark")')&&loader.includes('kind:"raster-hq-v10"')&&!loader.includes('new MutationObserver'),'HQ art loader missing');
ok(ux.includes('const FULL_ARK = hq("ark")')&&ux.includes('art.symbols.ark = FULL_ARK')&&ux.includes('board.dataset.rows')&&ux.includes('board.dataset.cols')&&ux.includes('fitBoardNow'),'V10 compatibility UX missing');ok(ux.includes('nodeImportant')&&ux.includes('addedNodes')&&!ux.includes('requestAnimationFrame(applyDragFrame)'),'compatibility UX should not animate icon position on every pointer move');ok(ux.includes('document.body.append(overlay)'),'modal portal missing');
ok(ui11.includes('window.__bmtV11UiInstalled')&&ui11.includes('loadArkRaster')&&ui11.includes('URL.createObjectURL(blob)')&&ui11.includes('probe.naturalWidth < 128'),'V11 full Ark raster preload missing');ok(ui11.includes('shell.querySelector(".bmt-hero-v2")?.remove()')&&ui11.includes('bmt-v11-tabs')&&ui11.includes('setupChapterTabs'),'V11 compact menu redesign missing');ok(ui11.includes('fitBoardNow')&&ui11.includes('--bmt-v11-board-width')&&ui11.includes('siblingsHeight'),'V11 single-viewport board fit missing');ok(ui11.includes('animateGhostPair')&&ui11.includes('bmt-v11-swap-layer')&&ui11.includes('is-swap-back'),'V11 continuous swap animation missing');ok(ui11.includes('img.decoding = "sync"')&&ui11.includes('predecodeArt'),'V11 decoded-piece anti-flicker missing');
ok(effects.includes('function lowPower()')&&effects.includes('count=Math.min(count,lowPower()?6:10)')&&!effects.includes('void buttonNode?.offsetWidth'),'mobile FX throttling missing');
ok(game.includes('let ROWS = 8')&&game.includes('resolveBoardRows')&&game.includes('--bmt-board-ratio')&&game.includes('currentSymbolAsset'),'variable board runtime missing');
ok(css10.includes('.bmt-board-active')&&css10.includes('--bmt-viewport-height')&&css10.includes('.bmt-prelevel__actions')&&css10.includes('.bmt-result-actions')&&css10.includes('overflow:hidden!important'),'V10 compatibility layout missing');
ok(css11.includes('.bmt-v11-dashboard')&&css11.includes('.bmt-v11-chapter-nav')&&css11.includes('.bmt-v11-pane.is-active'),'V11 menu styles missing');ok(css11.includes('position:fixed!important')&&css11.includes('--bmt-v11-board-width')&&css11.includes('.bmt-booster-tray')&&css11.includes('.bmt-actions-v2'),'V11 full-screen board/controls styles missing');ok(css11.includes('.bmt-v11-swap-layer')&&css11.includes('.bmt-v11-swap-ghost')&&css11.includes('.bmt-tile.is-swapping .bmt-piece-wrap'),'V11 swap continuity styles missing');
ok(launcher.includes('VERSION="11"')&&launcher.includes('biblical-match-three-v11-ui.js')&&launcher.includes('biblical-match-three-v11.css')&&launcher.includes('Матч‑3 с Путём света'),'V11 launcher missing');
for(const f of ['web/assets/biblical-match-three/hq-v5/symbols/fish.webp','web/assets/biblical-match-three/hq-v5/symbols/ark.webp','web/assets/biblical-match-three/ark.svg','web/games/biblical-match-three-v11-ui.js','web/styles/biblical-match-three-v11.css'])ok(fs.existsSync(path.join(root,f)),`${f} missing`);
const syms=['bible','fish','dove','lamp','crown','ark'];for(const rows of [5,6,7,8])for(let seed=1;seed<=30;seed++){let s=seed+rows*101;const rng=()=>{s=(s+0x6D2B79F5)|0;let t=Math.imul(s^(s>>>15),1|s);t=(t+Math.imul(t^(t>>>7),61|t))^t;return((t^(t>>>14))>>>0)/4294967296};const b=Core.createBoard(rows,8,syms,rng);ok(b.length===rows*8&&Core.findMatches(b,rows,8).length===0&&Core.findHint(b,rows,8),`${rows}x8 seed ${seed} invalid`)}
console.log('OK: Biblical Treasures V11 static checks passed');