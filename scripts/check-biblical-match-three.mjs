import fs from 'node:fs';import path from 'node:path';import {createRequire} from 'node:module';
const root=process.cwd(),require=createRequire(import.meta.url),Core=require(path.join(root,'web/games/biblical-match-three-core.js'));const read=p=>fs.readFileSync(path.join(root,p),'utf8'),exists=p=>fs.existsSync(path.join(root,p)),ok=(v,m)=>{if(!v)throw new Error(m)};
const data=JSON.parse(read('web/data/biblical_match_three_levels.json'));ok(data.rows===8&&data.cols===8,'8-column default required');ok(data.levels?.length===30,'30 levels required');const rowSet=new Set(data.levels.map(level=>Number(level.rows||data.rows)));ok([5,6,7,8].every(rows=>rowSet.has(rows)),'varied 5/6/7/8-row levels required');for(const level of data.levels){const rows=Number(level.rows||data.rows);ok(rows>=5&&rows<=8,`level ${level.id} rows invalid`);for(const group of level.blockers||[])for(const cell of group.cells||[])ok(Number(cell)>=0&&Number(cell)<rows*data.cols,`level ${level.id} blocker ${cell} outside ${rows}x${data.cols}`)}
const core=read('web/games/biblical-match-three-core.js'),runtime=read('web/games/biblical-match-three-v10-runtime.js'),loader=read('web/games/biblical-match-three-v5-loader.js'),effects=read('web/games/biblical-match-three-effects.js'),game=read('web/games/biblical-match-three.js'),ui15=read('web/games/biblical-match-three-v15-ui.js'),polish15=read('web/games/biblical-match-three-v15-polish.js'),css15=read('web/styles/biblical-match-three-v15-polish.css'),launcher=read('web/js/biblical-match-three-launcher.js');
ok(runtime.includes('TIMED_SECONDS = 90')&&runtime.includes('setInterval(updateTimedHud, 500)'),'timed mode / low-frequency timer missing');ok(runtime.includes('nodeImportant')&&runtime.includes('addedNodes')&&!runtime.includes('attributeFilter: ["class"'),'runtime observer must ignore animation attribute churn');
ok(loader.includes('kind:"file-webp-v17"')&&loader.includes('transport:"file"')&&loader.includes('icons-v17/ark.webp')&&loader.includes('icons-v17/bible.webp'),'V17 direct WebP loader missing');ok(!loader.includes('canvas.toDataURL')&&!loader.includes('canvas.toBlob')&&!loader.includes('URL.createObjectURL'),'V17 must use ordinary files instead of generated data/blob URLs');ok(!loader.includes('hq-v5/atlas-')&&!loader.includes('FALLBACK_BASE')&&!loader.includes('safe-fallback'),'legacy atlas/fallback still referenced');
ok(ui15.includes('window.__bmtV15UiInstalled')&&ui15.includes('attachSwipe')&&ui15.includes('fitBoardNow')&&ui15.includes('bmt-v13-menu'),'full-screen UI/swipe missing');ok(!ui15.includes('ARK_URL')&&!ui15.includes('patchArkImages'),'UI must not override direct Ark art');
ok(polish15.includes('webp-v17')&&polish15.includes('patchBoardPieces')&&polish15.includes('patchBoosters'),'V17 image patching missing');ok(css15.includes('.bmt-tile.is-swapping')&&css15.includes('overflow:visible!important')&&css15.includes('contain:none!important'),'swipe clipping fix missing');ok(css15.includes('background:transparent!important')&&css15.includes('webp-v17'),'transparent direct-WebP tile style missing');
ok(effects.includes('function lowPower()')&&effects.includes('count=Math.min(count,lowPower()?6:10)'),'mobile FX throttling missing');ok(game.includes('let ROWS = 8')&&game.includes('resolveBoardRows')&&game.includes('currentSymbolAsset'),'variable board runtime missing');
ok(launcher.includes('VERSION="20"')&&launcher.includes('ALLOWED_USER_ID="1288379477"')&&launcher.includes('5693086211')&&launcher.includes('5502223852')&&launcher.includes('isAllowedUser')&&launcher.includes('removeMenuCard'),'V20 three-user private access gate missing');ok(launcher.includes('file-webp-v17')&&launcher.includes('transport!=="file"'),'V17 launcher must require direct WebP art');
const icons=['ark','bible','bread','candle','chains','covenant','crown','dove','fish','grapes','jericho','score','sling','staff','tablets'];for(const name of icons)ok(exists(`web/assets/biblical-match-three/icons-v17/${name}.webp`),`V17 ${name}.webp missing`);
for(const f of ['web/assets/biblical-match-three/hq-v5/atlas-00.txt','web/assets/biblical-match-three/hq-v5/atlas-10.txt','web/assets/biblical-match-three/hq-v5/symbols/fish.webp'])ok(!exists(f),`obsolete V16 art must be deleted: ${f}`);
for(const f of ['web/games/biblical-match-three-v15-ui.js','web/games/biblical-match-three-v15-polish.js','web/styles/biblical-match-three-v15-polish.css'])ok(exists(f),`${f} missing`);ok(!exists('web/games/biblical-match-three-v14-raster-pack.js'),'V14 low-quality raster pack must stay deleted');
const campaignSymbols=['bible','fish','dove','lamp','crown','ark','bread','grapes','tablets'];
const levelShapes={1:'rect',2:'rect',3:'oval',4:'bowl',5:'diamond',6:'oval',7:'cross',8:'bowl',9:'diamond',10:'cross',11:'shield',12:'oval',13:'diamond',14:'bowl',15:'cross',16:'shield',17:'cross',18:'diamond',19:'bowl',20:'shield',21:'diamond',22:'cross',23:'bowl',24:'diamond',25:'shield',26:'cross',27:'bowl',28:'diamond',29:'shield',30:'cross'};
const maskFor=(shape,rows,cols,level)=>{const mask=new Array(rows*cols).fill(true),cx=(cols-1)/2,cy=(rows-1)/2;for(let row=0;row<rows;row++)for(let col=0;col<cols;col++){const dx=Math.abs(col-cx),dy=Math.abs(row-cy);let active=true;if(shape==='oval')active=((col-cx)/Math.max(1,cols*.53))**2+((row-cy)/Math.max(1,rows*.57))**2<=1;else if(shape==='diamond')active=dx/Math.max(1,cols*.52)+dy/Math.max(1,rows*.55)<=1;else if(shape==='cross')active=dx<=1.55||dy<=1.15;else if(shape==='bowl'){const edge=dx/Math.max(1,cx),minRow=Math.floor(edge*edge*Math.max(1,rows*.38));active=row>=minRow}else if(shape==='shield'){const t=rows<=1?0:row/(rows-1),half=t<.42?cols*.46:Math.max(1.35,cols*.46-(t-.42)*cols*.52);active=dx<=half}mask[row*cols+col]=active}for(const group of level.blockers||[])for(const rawIndex of group.cells||[]){const index=Number(rawIndex),row=Math.floor(index/cols),col=index%cols;mask[index]=true;for(const [dr,dc] of [[-1,0],[1,0],[0,-1],[0,1]]){const nr=row+dr,nc=col+dc;if(nr>=0&&nr<rows&&nc>=0&&nc<cols)mask[nr*cols+nc]=true}}return mask};
for(const level of data.levels){const required=[...new Set((level.goals||[]).filter(g=>g.type==='collect').map(g=>g.symbol))];let pool=campaignSymbols.slice(0,Math.max(3,Math.min(campaignSymbols.length,Number(level.symbolCount||6))));for(const symbol of required){if(pool.includes(symbol))continue;let slot=-1;for(let i=pool.length-1;i>=0;i--)if(!required.includes(pool[i])){slot=i;break}if(slot>=0)pool[slot]=symbol;else pool.push(symbol)}for(const symbol of required)ok(pool.includes(symbol),`level ${level.id} target ${symbol} absent from symbol pool`);for(const goal of level.goals||[]){if(goal.type==='clearBlockers'){const total=(level.blockers||[]).filter(b=>b.type===goal.blocker).reduce((sum,b)=>sum+(b.cells||[]).length,0);ok(total>=Number(goal.count||0),`level ${level.id} blocker goal impossible`)}if(goal.type==='lightLamps'){const total=(level.blockers||[]).filter(b=>b.type==='lamp').reduce((sum,b)=>sum+(b.cells||[]).length,0);ok(total>=Number(goal.count||0),`level ${level.id} lamp goal impossible`)}}const rows=Number(level.rows||data.rows),mask=maskFor(levelShapes[level.id]||'rect',rows,data.cols,level);ok(mask.filter(Boolean).length>=24,`level ${level.id} shaped board too small`);for(const group of level.blockers||[])for(const cell of group.cells||[]){const index=Number(cell),row=Math.floor(index/data.cols),col=index%data.cols;ok(mask[index],`level ${level.id} blocker placed in a hole`);const neighbors=[[-1,0],[1,0],[0,-1],[0,1]].map(([dr,dc])=>[row+dr,col+dc]).filter(([r,c])=>r>=0&&r<rows&&c>=0&&c<data.cols).map(([r,c])=>r*data.cols+c);ok(neighbors.some(i=>mask[i]),`level ${level.id} blocker ${index} isolated by board shape`)};for(let seed=1;seed<=12;seed++){let s=seed+level.id*997;const rng=()=>{s=(s+0x6D2B79F5)|0;let t=Math.imul(s^(s>>>15),1|s);t=(t+Math.imul(t^(t>>>7),61|t))^t;return((t^(t>>>14))>>>0)/4294967296};let playable=false;for(let attempt=0;attempt<30&&!playable;attempt++){const board=Core.createBoard(rows,data.cols,pool,rng);board.forEach((_,i)=>{if(!mask[i])board[i]=null});playable=Core.findMatches(board,rows,data.cols).length===0&&Core.findMoves(board,rows,data.cols,(a,b)=>mask[a]&&mask[b],3).length>=3&&required.every(sym=>board.filter(cell=>cell?.type===sym).length>=3)}ok(playable,`level ${level.id} seed ${seed} cannot produce playable shaped board`)}}
ok(game.includes('getLevelSymbolSet')&&game.includes('LEVEL_SHAPES')&&game.includes('createPlayableBoard')&&game.includes('freeChallengeConfig'),'V18 campaign rules missing');ok(game.includes('moves: 30')&&game.includes('rows: 7')&&game.includes('rows: 8'),'V18 free board sizing/move limits missing');ok(game.includes('is-blocker-breaking')&&css15.includes('bmt-v18-blocker-break'),'V18 obstacle animation missing');ok(game.includes('currentBlockerAsset')&&game.includes('bmt-blocker-art')&&game.includes('data-blocker-type=\"chain\"'),'V19 obstacle WebP markup missing');ok(core.includes('function findMoves')&&typeof Core.findMoves==='function','V20 move enumeration missing');ok(game.includes('const MIN_START_MOVES = 3')&&game.includes('function finishIfNoMoves()')&&game.includes('countPlayableMoves(runtime.board, 1) !== 0')&&game.includes('finishLevel(false, \"noMoves\")')&&game.includes('openFreeExit(\"noMoves\")')&&game.includes('checkDeadBoard:finishIfNoMoves'),'V20 start/dead-board rules missing');ok(!game.includes('Поле перемешано — появился новый доступный ход'),'automatic dead-board reshuffle must stay removed');ok(css15.includes('V19: obstacle artwork')&&css15.includes('.bmt-blocker-art')&&css15.includes('.bmt-blocker__layers'),'V19 visible obstacle styling missing');ok(css15.includes('V20: a lit lamp')&&css15.includes('.bmt-tile.is-lamp-lit .bmt-blocker-art{display:none!important')&&css15.includes('animation:none!important'),'V20 lit-lamp settle styling missing');ok(css15.includes('.bmt-tile.is-hole')&&css15.includes('width:62px'),'V18 shaped board/free icon polish missing');
const syms=['bible','fish','dove','lamp','crown','ark'];for(const rows of [5,6,7,8])for(let seed=1;seed<=30;seed++){let s=seed+rows*101;const rng=()=>{s=(s+0x6D2B79F5)|0;let t=Math.imul(s^(s>>>15),1|s);t=(t+Math.imul(t^(t>>>7),61|t))^t;return((t^(t>>>14))>>>0)/4294967296};const b=Core.createBoard(rows,8,syms,rng);ok(b.length===rows*8&&Core.findMatches(b,rows,8).length===0&&Core.findMoves(b,rows,8,null,3).length>=3,`${rows}x8 seed ${seed} has fewer than 3 starting moves`)}
console.log('OK: Biblical Treasures V20 stable lamps + 3-start-move + dead-board finish checks passed');
