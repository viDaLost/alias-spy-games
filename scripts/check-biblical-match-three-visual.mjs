import http from 'node:http';
import fs from 'node:fs';
import path from 'node:path';
import { chromium } from 'playwright-core';

const root=process.cwd();
const V='42';
const QA_USER='1288379477';
// V45 replaced the Noah's Ark booster with the Rainbow one. That change used to be
// applied by the launcher at runtime, so this page -- which loads the game source
// directly -- was the only place the Ark booster still appeared. The change is in
// the source now, so the rail shows what players actually see.
const scripts=[
  'biblical-match-three-v5-loader.js','biblical-match-three-core.js','biblical-match-three-progress.js','biblical-match-three-effects.js','biblical-match-three.js','biblical-match-three-v10-runtime.js','biblical-match-three-v15-ui.js','biblical-match-three-v15-polish.js',
].map(name=>`<script src="/web/games/${name}?v=${V}"></script>`).join('');
// The ten v2..v21-art layers were merged into one stylesheet; this page must load
// what the launcher loads, or it renders the game unstyled and every geometry
// assertion below measures an unstyled button.
const styles=`<link rel="stylesheet" href="/web/styles/biblical-match-three.css?v=${V}">`;
const strictCsp=`default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self'; connect-src 'self';`;
const html=`<!doctype html><html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta http-equiv="Content-Security-Policy" content="${strictCsp}">${styles}</head><body data-current-game="biblical-match-three" data-mode="game"><main id="game-container"></main><script>
window.Telegram={WebApp:{initDataUnsafe:{user:{id:Number('${QA_USER}')}},contentSafeAreaInset:{top:96},safeAreaInset:{top:47},HapticFeedback:{selectionChanged(){},notificationOccurred(){}}}};window.appGoToMainMenu=()=>{};localStorage.setItem('bible_stars_v1_${QA_USER}','100');
</script>${scripts}<script>(async()=>{await window.BiblicalMatchThreeV5ArtReady;await window.startBiblicalMatchThreeGame('/web/data/biblical_match_three_levels.json?v=${V}')})().catch(e=>{document.body.dataset.qaError=String(e&&e.stack||e)})</script></body></html>`;
function accessHtml(uid){return `<!doctype html><html lang="ru"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta http-equiv="Content-Security-Policy" content="${strictCsp}"></head><body data-mode="menu"><main id="menu-container"><section><div id="kids-games"></div></section></main><main id="game-container"></main><script>window.Telegram={WebApp:{initDataUnsafe:{user:{id:Number('${uid}')}},contentSafeAreaInset:{top:0},safeAreaInset:{top:0},HapticFeedback:{selectionChanged(){},notificationOccurred(){}}}};window.appGoToMainMenu=()=>{};</script><script src="/web/js/biblical-match-three-launcher.js?v=${V}"></script></body></html>`}
const mime={'.js':'text/javascript','.css':'text/css','.json':'application/json','.svg':'image/svg+xml','.webp':'image/webp','.png':'image/png','.txt':'text/plain'};
const server=http.createServer((req,res)=>{const url=new URL(req.url||'/','http://local');if(url.pathname==='/__qa'){res.writeHead(200,{'Content-Type':'text/html','Cache-Control':'no-store'});res.end(html);return}if(url.pathname==='/__access'){res.writeHead(200,{'Content-Type':'text/html','Cache-Control':'no-store'});res.end(accessHtml(url.searchParams.get('uid')||'0'));return}const file=path.resolve(root,'.'+decodeURIComponent(url.pathname));if(!file.startsWith(root+path.sep)||!fs.existsSync(file)||!fs.statSync(file).isFile()){res.writeHead(404).end();return}res.writeHead(200,{'Content-Type':mime[path.extname(file)]||'application/octet-stream','Cache-Control':'no-store'});fs.createReadStream(file).pipe(res)});
await new Promise(resolve=>server.listen(0,'127.0.0.1',resolve));
const base=`http://127.0.0.1:${server.address().port}`;
const browser=await chromium.launch({headless:true,executablePath:process.env.CHROME_BIN||'/usr/bin/google-chrome',args:['--no-sandbox','--disable-dev-shm-usage']});
const context=await browser.newContext({viewport:{width:390,height:844},isMobile:true,hasTouch:true});

async function checkAccess(){
 const guest=await context.newPage(),guestRequests=[];guest.on('request',r=>guestRequests.push(r.url()));await guest.goto(`${base}/__access?uid=999999999`,{waitUntil:'domcontentloaded',timeout:30000});await guest.waitForSelector('#biblical-match-three-card',{timeout:20000});
 const guestState=await guest.evaluate(()=>({allowed:window.BiblicalMatchThreeAccess?.isAllowedUser?.(),publicAccess:window.BiblicalMatchThreeAccess?.publicAccess,id:window.BiblicalMatchThreeAccess?.currentUserId?.(),src:document.querySelector('#biblical-match-three-card img')?.getAttribute('src')||''}));
 if(!guestState.allowed||!guestState.publicAccess||guestState.id!=='999999999'||!guestState.src.includes('assets/icons/biblical-treasures-v38.webp'))throw new Error(`public guest access ${JSON.stringify(guestState)}`);
 if(guestRequests.some(url=>/biblical-match-three-v5-loader|icons-v17\//i.test(url)))throw new Error('public launcher eagerly loaded gameplay art before the card was opened');
 await guest.locator('#biblical-match-three-card').click();await guest.waitForSelector('.bmt-v13-menu',{timeout:20000});if(!guestRequests.some(url=>/biblical-match-three-v5-loader/i.test(url))||!guestRequests.some(url=>/icons-v17\/bible\.webp/i.test(url)))throw new Error('public guest did not load V17 game art');await guest.close();
 const member=await context.newPage();await member.goto(`${base}/__access?uid=${QA_USER}`,{waitUntil:'domcontentloaded',timeout:30000});await member.waitForSelector('#biblical-match-three-card',{timeout:20000});
 const memberState=await member.evaluate(()=>({allowed:window.BiblicalMatchThreeAccess?.isAllowedUser?.(),publicAccess:window.BiblicalMatchThreeAccess?.publicAccess,id:window.BiblicalMatchThreeAccess?.currentUserId?.()}));
 if(!memberState.allowed||!memberState.publicAccess||memberState.id!==QA_USER)throw new Error(`public member access ${JSON.stringify(memberState)}`);await member.close();
}
async function dismissTutorial(page){await page.waitForTimeout(700);const button=page.locator('.bmt-tutorial button').first();if(await button.count()){try{if(await button.isVisible())await button.click()}catch{}}await page.waitForTimeout(180)}
async function checkGame(){
 const page=await context.newPage(),requested=[];page.on('request',request=>requested.push(request.url()));
 try{
  await page.goto(base+'/__qa',{waitUntil:'domcontentloaded',timeout:30000});await page.waitForSelector('.bmt-v13-menu',{timeout:20000});await page.waitForFunction(()=>window.BiblicalMatchThreeV5Art?.kind==='file-webp-v17'&&window.BiblicalMatchThreeV5Art?.transport==='file'&&window.__bmtV15UiInstalled&&window.__bmtV15PolishInstalled,{timeout:15000});
  const menu=await page.evaluate(async()=>{const art=window.BiblicalMatchThreeV5Art;const groups={symbols:art.symbols,boosters:art.boosters,goals:art.goals,obstacles:art.obstacles};const sources=Object.entries(groups).flatMap(([group,map])=>Object.entries(map).map(([key,src])=>({group,key,src})));const decoded=await Promise.all(sources.map(item=>new Promise(resolve=>{const img=new Image();let done=false;const finish=()=>{if(done)return;done=true;resolve({...item,w:img.naturalWidth,h:img.naturalHeight})};img.onload=finish;img.onerror=finish;img.src=item.src;if(img.complete)setTimeout(finish,0)})));return{kind:art.kind,transport:art.transport,ark:art.symbols?.ark||'',boosterArk:art.boosters?.ark||'',decoded,overflow:Math.max(document.documentElement.scrollWidth,document.body.scrollWidth)-innerWidth}});
  const goodSrc=src=>String(src||'').includes('/icons-v17/')&&/\.webp(?:\?|$)/i.test(String(src));
  if(menu.kind!=='file-webp-v17'||menu.transport!=='file'||!goodSrc(menu.ark)||!goodSrc(menu.boosterArk)||menu.overflow>2)throw new Error(`menu ${JSON.stringify({...menu,decoded:undefined})}`);
  if(menu.decoded.length<20||menu.decoded.some(x=>!goodSrc(x.src)||x.w<64||x.h<64))throw new Error(`all-art ${JSON.stringify(menu.decoded.filter(x=>!goodSrc(x.src)||x.w<64||x.h<64))}`);
  const firstLevel=page.locator('.bmt-v13-chapter.is-active .bmt-v13-level:not([disabled]),.bmt-v13-chapter.is-active .bmt-journey-node:not([disabled]),.bmt-v13-level:not([disabled])').first();await firstLevel.click();await page.waitForSelector('.bmt-prelevel',{state:'visible',timeout:6000});await page.waitForTimeout(260);
  const prelevel=await page.evaluate(()=>{const root=document.querySelector('.bmt-prelevel');const goals=[...(root?.querySelectorAll('.bmt-prelevel__goals img,.bmt-goal__icon img')||[])].map(img=>({src:img.getAttribute('src')||'',w:img.naturalWidth,tag:img.dataset.bmtRaster,visibility:getComputedStyle(img).visibility,opacity:+getComputedStyle(img).opacity}));const start=[...document.querySelectorAll('button')].find(btn=>(btn.textContent||'').includes('Начать уровень'));const r=start?.getBoundingClientRect();return{goals,start:start?{disabled:start.disabled,display:getComputedStyle(start).display,visibility:getComputedStyle(start).visibility,opacity:+getComputedStyle(start).opacity,w:r.width,h:r.height,bottom:r.bottom}:null}});
  if(!prelevel.goals.length||prelevel.goals.some(x=>!goodSrc(x.src)||x.w<64||x.tag!=='webp-v17'||x.visibility==='hidden'||x.opacity<.99))throw new Error(`level-one goals ${JSON.stringify(prelevel.goals)}`);if(!prelevel.start||prelevel.start.disabled||prelevel.start.display==='none'||prelevel.start.visibility==='hidden'||prelevel.start.opacity<.99||prelevel.start.w<80||prelevel.start.h<34||prelevel.start.bottom>846)throw new Error(`level-one start ${JSON.stringify(prelevel.start)}`);
  await page.getByRole('button',{name:/Начать уровень/}).click();await page.waitForSelector('.bmt-board .bmt-tile',{timeout:8000});await dismissTutorial(page);await page.waitForFunction(()=>[...document.querySelectorAll('.bmt-piece')].filter(img=>img.src).every(img=>img.complete&&img.naturalWidth>=64&&img.dataset.bmtRaster==='webp-v17'),{timeout:8000});
  const artState=await page.evaluate(()=>({rows:Number(document.querySelector('.bmt-board')?.dataset.rows||0),cols:Number(document.querySelector('.bmt-board')?.dataset.cols||0),tiles:document.querySelectorAll('.bmt-board .bmt-tile').length,pieces:[...document.querySelectorAll('.bmt-piece')].filter(img=>img.src).map(img=>({src:img.getAttribute('src'),w:img.naturalWidth,tag:img.dataset.bmtRaster,visibility:getComputedStyle(img).visibility})),boosters:[...document.querySelectorAll('.bmt-booster__icon img')].map(img=>({src:img.getAttribute('src'),w:img.naturalWidth,tag:img.dataset.bmtRaster,visibility:getComputedStyle(img).visibility})),ark:[...document.querySelectorAll('[data-booster="rainbow"] img')].map(img=>({src:img.getAttribute('src'),w:img.naturalWidth,tag:img.dataset.bmtRaster})),tileBg:getComputedStyle(document.querySelector('.bmt-tile')).backgroundColor}));
  if(artState.rows!==5||artState.cols!==8||artState.tiles!==40)throw new Error(`level-one board ${JSON.stringify({rows:artState.rows,cols:artState.cols,tiles:artState.tiles})}`);if(artState.tileBg!=='rgba(0, 0, 0, 0)')throw new Error(`tile background ${artState.tileBg}`);if(!artState.pieces.length||artState.pieces.some(x=>!goodSrc(x.src)||x.w<64||x.tag!=='webp-v17'||x.visibility==='hidden'))throw new Error(`pieces ${JSON.stringify(artState.pieces.slice(0,8))}`);if(artState.boosters.length!==4||artState.boosters.some(x=>!goodSrc(x.src)||x.w<64||x.tag!=='webp-v17'||x.visibility==='hidden'))throw new Error(`boosters ${JSON.stringify(artState.boosters)}`);if(artState.ark.length!==1||!goodSrc(artState.ark[0].src)||artState.ark[0].w<64||artState.ark[0].tag!=='webp-v17')throw new Error(`ark ${JSON.stringify(artState.ark)}`);
  const first=page.locator('.bmt-tile').first(),box=await first.boundingBox();await page.mouse.move(box.x+box.width*.5,box.y+box.height*.5);await page.mouse.down();await page.mouse.move(box.x+box.width*1.15,box.y+box.height*.5,{steps:3});await page.mouse.up();await page.waitForSelector('.bmt-tile.is-swapping',{timeout:2500});
  const during=await page.evaluate(()=>[...document.querySelectorAll('.bmt-tile.is-swapping')].map(tile=>{const img=tile.querySelector('.bmt-piece'),wrap=tile.querySelector('.bmt-piece-wrap'),ts=getComputedStyle(tile),is=getComputedStyle(img),ws=getComputedStyle(wrap);return{overflow:ts.overflow,contain:ts.contain,visibility:is.visibility,opacity:+is.opacity,w:img.naturalWidth,src:img.getAttribute('src'),wrapOverflow:ws.overflow}}));if(during.length!==2||during.some(x=>x.overflow!=='visible'||x.contain!=='none'||x.visibility==='hidden'||x.opacity<.99||x.w<64||!goodSrc(x.src)||x.wrapOverflow!=='visible'))throw new Error(`swipe ${JSON.stringify(during)}`);
  await page.waitForFunction(()=>!document.querySelector('.bmt-tile.is-swapping')&&!document.querySelector('.bmt-tile.is-swap-back'),{timeout:3000});
  // A swap can cascade in fresh pieces whose <img> is still decoding. Without waiting
  // for them, the count below catches naturalWidth===0 on a perfectly healthy board,
  // which made this check fail at random.
  await page.waitForFunction(()=>[...document.querySelectorAll('.bmt-piece')].every(img=>!img.getAttribute('src')||(img.complete&&img.naturalWidth>0)),{timeout:8000});
  const after=await page.evaluate(()=>({missing:[...document.querySelectorAll('.bmt-piece')].filter(img=>img.src&&(img.naturalWidth===0||getComputedStyle(img).visibility==='hidden')).length,scrollH:document.documentElement.scrollHeight,innerH:innerHeight,dataImages:[...document.images].filter(img=>/^(?:data:|blob:)/i.test(img.getAttribute('src')||'')).length}));if(after.missing||after.dataImages||after.scrollH>after.innerH+3)throw new Error(`after ${JSON.stringify(after)}`);
  if(requested.some(url=>/hq-v5\/atlas-|hq-v5\/symbols\/fish|biblical-match-three-(?:v14-raster-pack|v14-icons|art-symbols|art-boosters|art-goals|art-obstacles)/i.test(url)))throw new Error('obsolete art requested');
 }finally{await page.close()}
}

async function checkV18Rules(){
 const page=await context.newPage();
 try{
  await page.goto(base+'/__qa?unlockAll=1',{waitUntil:'domcontentloaded',timeout:30000});await page.waitForSelector('.bmt-v13-menu',{timeout:20000});
  const level3=page.locator('.bmt-v13-level').filter({hasText:'Хлеб жизни'}).first();await level3.evaluate(el=>el.click());await page.waitForSelector('.bmt-prelevel',{state:'visible',timeout:6000});await page.getByRole('button',{name:/Начать уровень/}).click();await page.waitForSelector('.bmt-board',{timeout:8000});await dismissTutorial(page);
  const l3=await page.evaluate(()=>({shape:document.querySelector('.bmt-board')?.dataset.shape,holes:document.querySelectorAll('.bmt-tile.is-hole').length,bread:[...document.querySelectorAll('.bmt-piece')].filter(img=>img.alt==='Хлеб'&&img.naturalWidth>=64).length,active:Number(document.querySelector('.bmt-board')?.dataset.activeCells||0),startMoves:Number(document.querySelector('.bmt-board')?.dataset.startMoves||0),apiMoves:window.BiblicalMatchThreeV20Rules?.countPlayableMoves?.()||0}));
  if(l3.shape!=='oval'||l3.holes<1||l3.bread<3||l3.active<24||l3.startMoves<3||l3.apiMoves<3)throw new Error(`level3 rules ${JSON.stringify(l3)}`);
  await page.locator('.bmt-gamebar .bmt-icon-button').click();await page.waitForSelector('.bmt-pause-overlay',{timeout:3000});await page.getByRole('button',{name:/В меню игры/}).click();await page.waitForSelector('.bmt-v13-menu',{timeout:5000});await page.locator('[data-v13-mode="free"]').click();
  const iconLayout=await page.evaluate(()=>[...document.querySelectorAll('.bmt-free-card')].map(card=>{const img=card.querySelector('.bmt-v15-free-icon'),label=card.querySelector('.bmt-free-card__mode'),ir=img?.getBoundingClientRect(),lr=label?.getBoundingClientRect();return{w:ir?.width||0,overlap:ir&&lr?!(ir.right<=lr.left||ir.left>=lr.right||ir.bottom<=lr.top||ir.top>=lr.bottom):false}}));
  if(iconLayout.length<4||iconLayout.some(x=>x.w<48||x.overlap))throw new Error(`free icon layout ${JSON.stringify(iconLayout)}`);
  const medium=page.locator('.bmt-free-card').filter({hasText:'Средний'}).first();await medium.click();await page.waitForSelector('.bmt-board',{timeout:5000});const med=await page.evaluate(()=>({rows:+document.querySelector('.bmt-board')?.dataset.rows,moves:document.getElementById('bmt-moves')?.textContent?.trim(),shape:document.querySelector('.bmt-board')?.dataset.shape,blockers:document.querySelectorAll('.bmt-tile.has-chain,.bmt-tile.has-tablet').length,startMoves:Number(document.querySelector('.bmt-board')?.dataset.startMoves||0)}));if(med.rows!==8||med.moves!=='30'||med.shape!=='bowl'||med.blockers<8||med.startMoves<3)throw new Error(`medium ${JSON.stringify(med)}`);
  await page.locator('.bmt-gamebar .bmt-icon-button').click();await page.waitForSelector('.bmt-result-overlay',{timeout:3000});await page.getByRole('button',{name:/В меню/}).click();await page.waitForSelector('.bmt-v13-menu',{timeout:5000});await page.locator('[data-v13-mode="free"]').click();const hard=page.locator('.bmt-free-card').filter({hasText:'Сложный'}).first();await hard.click();await page.waitForSelector('.bmt-board',{timeout:5000});const hardState=await page.evaluate(()=>({rows:+document.querySelector('.bmt-board')?.dataset.rows,moves:document.getElementById('bmt-moves')?.textContent?.trim(),shape:document.querySelector('.bmt-board')?.dataset.shape,blockers:document.querySelectorAll('.bmt-tile.has-chain,.bmt-tile.has-tablet,.bmt-tile.has-lamp').length,startMoves:Number(document.querySelector('.bmt-board')?.dataset.startMoves||0),obstacles:[...document.querySelectorAll('.bmt-blocker-art')].map(img=>({src:img.getAttribute('src')||'',w:img.naturalWidth,h:img.naturalHeight,tag:img.dataset.bmtRaster,visibility:getComputedStyle(img).visibility,opacity:+getComputedStyle(img).opacity}))}));if(hardState.rows!==8||hardState.moves!=='30'||hardState.shape!=='cross'||hardState.blockers<14||hardState.startMoves<3)throw new Error(`hard ${JSON.stringify(hardState)}`);if(hardState.obstacles.length<14||hardState.obstacles.some(x=>!String(x.src||'').includes('/icons-v17/')||!/\.webp(?:\?|$)/i.test(String(x.src||''))||x.w<64||x.h<64||x.tag!=='webp-v17'||x.visibility==='hidden'||x.opacity<.95)||!hardState.obstacles.some(x=>/chains\.webp/.test(x.src))||!hardState.obstacles.some(x=>/tablets\.webp/.test(x.src))||!hardState.obstacles.some(x=>/candle\.webp/.test(x.src)))throw new Error(`hard obstacle art ${JSON.stringify(hardState.obstacles)}`);
  const lamp=page.locator('.bmt-tile.has-lamp').first();const lampIndex=await lamp.getAttribute('data-index');if(lampIndex==null)throw new Error('hard mode has no lamp target');await page.locator('[data-booster="sling"]').click();await page.waitForFunction(()=>document.querySelector('[data-booster="sling"]')?.classList.contains('is-active'),{timeout:3000});await page.evaluate(index=>document.querySelector(`.bmt-tile[data-index="${index}"]`)?.click(),lampIndex);try{await page.waitForFunction(index=>document.querySelector(`.bmt-tile[data-index="${index}"]`)?.classList.contains('is-lamp-lit'),lampIndex,{timeout:7000})}catch(error){const diagnostic=await page.evaluate(index=>{const tile=document.querySelector(`.bmt-tile[data-index="${index}"]`),booster=document.querySelector('[data-booster="sling"]'),shell=document.querySelector('.bmt-shell');return{tileClass:tile?.className||'',boosterClass:booster?.className||'',shellClass:shell?.className||'',wallet:document.querySelector('[data-bmt-wallet]')?.textContent||'',result:document.querySelector('.bmt-result-card h3')?.textContent||''}},lampIndex);throw new Error(`lamp did not light ${JSON.stringify(diagnostic)}; ${error.message}`)}await page.waitForTimeout(420);const litLamp=await page.evaluate(index=>{const tile=document.querySelector(`.bmt-tile[data-index="${index}"]`),wrapper=tile?.querySelector('.bmt-blocker__lamp'),piece=tile?.querySelector('.bmt-piece-wrap'),state=tile?.querySelector('.bmt-blocker__lamp-state'),sr=state?.getBoundingClientRect();return{art:tile?.querySelectorAll('.bmt-blocker-art').length||0,animation:wrapper?getComputedStyle(wrapper).animationName:'',pieceOpacity:piece?+getComputedStyle(piece).opacity:0,stateW:sr?.width||0,stateH:sr?.height||0}},lampIndex);if(litLamp.art!==0||litLamp.animation!=='none'||litLamp.pieceOpacity<.99||litLamp.stateW<15||litLamp.stateH<15)throw new Error(`lit lamp ${JSON.stringify(litLamp)}`);
 }finally{await page.close()}
}

async function checkNoMoveTermination(){
 const page=await context.newPage();
 try{
  await page.goto(base+'/__qa',{waitUntil:'domcontentloaded',timeout:30000});await page.waitForSelector('.bmt-v13-menu',{timeout:20000});const first=page.locator('.bmt-v13-chapter.is-active .bmt-v13-level:not([disabled]),.bmt-v13-chapter.is-active .bmt-journey-node:not([disabled]),.bmt-v13-level:not([disabled]):visible').first();await first.scrollIntoViewIfNeeded();await first.click();await page.waitForSelector('.bmt-prelevel',{state:'visible',timeout:6000});await page.getByRole('button',{name:/Начать уровень/}).click();await page.waitForSelector('.bmt-board',{timeout:8000});await dismissTutorial(page);const startMoves=await page.evaluate(()=>window.BiblicalMatchThreeV20Rules?.countPlayableMoves?.()||0);if(startMoves<3)throw new Error(`campaign start moves ${startMoves}`);const ended=await page.evaluate(()=>{window.BiblicalMatchThreeCore.findMoves=()=>[];return window.BiblicalMatchThreeV20Rules?.checkDeadBoard?.()});if(ended!==true)throw new Error(`dead-board check returned ${String(ended)}`);await page.waitForSelector('.bmt-result-overlay',{state:'visible',timeout:3000});const result=await page.evaluate(()=>({title:document.querySelector('.bmt-result-card h3')?.textContent?.trim()||'',continueButtons:[...document.querySelectorAll('.bmt-result-card button')].filter(b=>(b.textContent||'').includes('+5 ходов')).length}));if(result.title!=='Нет доступных ходов'||result.continueButtons)throw new Error(`campaign no-move result ${JSON.stringify(result)}`);
 }finally{await page.close()}
}

try{await checkAccess();await checkGame();await checkV18Rules();await checkNoMoveTermination();console.log('OK: Biblical Treasures V20 settled lamps + 3 starting moves + dead-board finish + mobile gameplay passed')}finally{await context.close();await browser.close();await new Promise(resolve=>server.close(resolve))}
